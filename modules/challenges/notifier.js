/**
 * @param {FruitPickerSuccess | FruitPickerError} message
 * @return void
 */
export function notify(message) {
  // This is a mocked function for use with the exercise.
  message;
}

export function notifyIt(message) {
  return message;
}